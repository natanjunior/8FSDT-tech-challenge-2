'use strict';

/**
 * As rotas POST/GET /posts/:id/read foram migradas para POST /reads e GET /reads/search.
 * Os testes correspondentes estão em reads.integration.test.js.
 *
 * Este arquivo é mantido apenas para referência histórica — os testes foram desabilitados.
 */

describe('PostReads (legacy — rotas migradas para /reads)', () => {
  test('ver reads.integration.test.js para os testes atualizados', () => {
    expect(true).toBe(true);
  });
});
